import { ShortUserDetails } from './short-user-details.model';

describe('ShortUserDetails', () => {
  it('should create an instance', () => {
    expect(new ShortUserDetails()).toBeTruthy();
  });
});
