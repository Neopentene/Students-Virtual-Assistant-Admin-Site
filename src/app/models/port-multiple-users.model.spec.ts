import { PortMultipleUsers } from './port-multiple-users.model';

describe('PortMultipleUsers', () => {
  it('should create an instance', () => {
    expect(new PortMultipleUsers()).toBeTruthy();
  });
});
